import styled from 'styled-components';

const Wrapper = styled.div`
  left: 50%;
  margin-left: -20px;
  top: 50%;
  margin-top: -20px;
  width: 40px;
  height: 40px;
  position: fixed;
`;

export default Wrapper;
