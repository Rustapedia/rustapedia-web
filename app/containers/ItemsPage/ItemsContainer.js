import styled from 'styled-components';

const ItemsContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  max-width: 1200px;
  margin: 0 auto;
  padding-top: 84px;
`;

export default ItemsContainer;
